# Supabase - Usuários (`public.usuarios`) e RLS

Este arquivo contém SQL para criar a tabela pública `public.usuarios` e políticas RLS básicas para o próprio usuário ler (e opcionalmente atualizar) seu perfil.

## SQL: create table + RLS

Veja o arquivo `SQL_usuarios_supabase.sql` na raiz do projeto (mantido como referência).

